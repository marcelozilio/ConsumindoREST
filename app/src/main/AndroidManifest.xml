<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.pwdinfo.consumindorest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".telas.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".telas.TelaInicial"
            android:label="@string/title_activity_tela_inicial"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".telas.TelaCadastro"
            android:label="Cadastro de Produtos" />
        <activity
            android:name=".telas.TelaBusca"
            android:label="Lista de Produtos" />
        <activity android:name=".telas.CadastroUsuario"
            android:label="Cadastro de Usuário"/>
    </application>

</manifest>