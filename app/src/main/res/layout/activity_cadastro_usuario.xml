<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".telas.CadastroUsuario">

    <EditText
        android:id="@+id/etLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="43dp"
        android:width="200dp"
        android:hint="Digite o login" />

    <EditText
        android:id="@+id/etSenha"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etLogin"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="38dp"
        android:width="200dp"
        android:ems="10"
        android:hint="Dogite a senha"
        android:inputType="textPassword" />

    <EditText
        android:id="@+id/etRepSenha"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etSenha"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="38dp"
        android:width="200dp"
        android:ems="10"
        android:hint="Repita a senha"
        android:inputType="textPassword" />

    <Button
        android:id="@+id/btnCadUsr"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:width="150dp"
        android:height="60dp"
        android:text="Finalizar"
        android:textSize="20dp"
        android:layout_marginTop="62dp"
        android:layout_below="@+id/etRepSenha"
        android:layout_centerHorizontal="true"
        android:textColor="@color/abc_btn_colored_borderless_text_material" />

</RelativeLayout>
